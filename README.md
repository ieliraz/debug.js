# debug.js
small and nice plugin to turn off and on debug mode

Example:

        $$.debugMode = true; // when false all the following methods not occur
        
        $$.pause(); // do debugger
        
        $$.log('test'); // do console.log
        
        $$.alert('test'); // do window.alert
        
